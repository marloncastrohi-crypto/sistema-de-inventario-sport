server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sportstock?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: castro2001
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

